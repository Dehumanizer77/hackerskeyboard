// Top-level build file. Repositories are declared centrally in settings.gradle.
plugins {
    id 'com.android.application' version '8.10.0' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}
